<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    
    <nav class="bg-gray-800 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Logo or brand name -->
            <a href="/" class="text-white text-lg font-semibold">fBay</a>
    
            <!-- Navbar items -->
            <div class="flex space-x-4">
                <!-- Ad link -->
                <a href="/api/newads" class="text-gray-300 hover:text-white">Sell</a>
    
                <!-- Profile link -->
                <a href="/api/profile" class="text-gray-300 hover:text-white">Profile</a>
    
                <!-- Logout link -->
                <form action="/logout" method="post" class="inline">
                    <button type="submit" class="text-gray-300 hover:text-white">Logout</button>
                </form>
            </div>
        </div>
    </nav>
 <div class="bg-gray-200">
    <div class="max-w-3xl mx-auto py-8 ">
        <h1 class="text-3xl font-semibold mb-4"><%= ad.title %></h1>
        <div class="mb-4">
            <img src="<%= ad.photoUrl %>" alt="<%= ad.title %>" class="w-full rounded-lg">
        </div>
        <p class="mb-4"><%= ad.description %></p>
        <p class="mb-4">Current Bid: <%= ad.currentBid %>Rs</p>
        <form action="/api/bid/<%= ad._id %>" method="post" class="mb-4">
            <input type="hidden" name="adId" value="<%= ad._id %>">
            <label for="bidAmount" class="block mb-2">Place Bid:</label>
            <input type="number" name="bidAmount" id="bidAmount" class="w-full px-4 py-2 rounded-lg border-gray-300 focus:outline-none focus:border-indigo-500">
            <button type="submit" class="mt-2 bg-indigo-500 text-white py-2 px-4 rounded-lg">Submit Bid</button>
        </form>
    </div>
</div>

<a href="/api/ads/<%=ad._id %>/bids" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
    Bid History
</a>

</body>
</html>